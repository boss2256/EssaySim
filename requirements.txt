streamlit==1.32.2
nltk>=3.7
pandas>=1.5.0
matplotlib>=3.5.0
seaborn>=0.12.0
scikit-learn>=1.0.0
python-docx>=0.8.11
scipy>=1.7.0

# Core NLP libraries
sentence-transformers==2.2.2  # Keeping this pinned as it's central
torch==2.2.0                 # Keeping this pinned

# Allow newer versions compatible with sentence-transformers and torch
transformers                 # <-- REMOVED version pin
huggingface_hub              # <-- REMOVED version pin

# REMOVED the following line entirely:
# tokenizers>=0.13.3,<0.15